// Create `.env.js` in the same directory with the following information populated
// (or leave as '' if seeding script is not required)
const config = {
	apiKey: '',
	authDomain: '',
	projectId: '',
	storageBucket: '',
	messagingSenderId: '',
	appId: '',
	measurementId: '',
}

module.exports = { config }
